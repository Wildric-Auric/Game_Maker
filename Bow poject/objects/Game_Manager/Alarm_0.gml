/// @description Insérez la description ici
// Vous pouvez écrire votre code dans cet éditeur
instance_create_layer(rand, 800, layer, object3);
if (timer > 120)
{
timer = timer - 5
}
else {timer = 120}
once = false;